import Layout from '@/components/Layout'

export default function Vote() {
  return (
    <Layout>
      <div className="card">
        <h1 className="text-2xl font-bold">Vote</h1>
        <p className="mt-2 text-dxrery.muted">Voting placeholders — replace VOTE_URL in environment variables.</p>
      </div>
    </Layout>
  )
}
